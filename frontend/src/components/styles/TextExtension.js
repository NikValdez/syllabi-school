import styled from 'styled-components'

const TextExtension = styled.p`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 10px;
  &:hover {
    color: grey;
  }
`
export default TextExtension
